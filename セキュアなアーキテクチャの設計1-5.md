# セキュアなアーキテクチャの設計

AWS Solution Architect Associate 試験対策問題集

---

## 問題 29

複数チームが共通のVPCを利用しています。Web層、アプリ層、データ層の通信制御を簡潔に保ちつつ、将来のインスタンス増減にも対応したい場合、最も適切なネットワーク制御はどれですか？また、監査部門からは、設定の一元管理とアクセス制御の明確化も求められており、運用チームは自前実装や手動運用をできるだけ増やしたくありません。

### 選択肢

**ア.** ルートテーブルを分けて各層間の通信方向を制御し、セキュリティグループはすべてのポートを許可する

**イ.** サブネットごとにNACLだけを使い、ポートとCIDRを詳細に定義して各層の通信を制御する

**ウ.** VPCを層ごとに分離し、通信制御はTransit Gatewayのルートテーブルだけで行う

**エ.** セキュリティグループを使って層ごとの通信相手を参照ベースで許可し、必要に応じてNACLは補助的に使う

<details>
<summary>解答と解説を表示</summary>

**正解:** エ. セキュリティグループを使って層ごとの通信相手を参照ベースで許可し、必要に応じてNACLは補助的に使う

#### 解説

スケーラブルで保守しやすい通信制御には、相手セキュリティグループ参照を使った設計が適しています。


#### 各選択肢のポイント


**ア.**

- 不正解。インスタンス単位の通信制御には使えますが、この設問で必要なサブネット境界の stateless なフィルタリングを直接満たせないため適していません。


**イ.**

- 不正解。サブネット境界の stateless なフィルタリングには使えますが、この設問で必要なサブネット境界の stateless なフィルタリングを直接満たせないため適していません。


**ウ.**

- 不正解。VPC やネットワーク間接続の集約には使えますが、この設問で必要なサブネット境界の stateless なフィルタリングを直接満たせないため適していません。


**エ.**

- 正解。スケーラブルで保守しやすい通信制御には、相手セキュリティグループ参照を使った設計が適しています。

</details>

---

## 問題 30

社内APIをAmazon API Gatewayで公開しています。外部パートナーには自社のIDプロバイダーで認証させつつ、API Gateway側では認可も統一的に扱いたい状況です。さらに、既存アプリケーションへの影響はできるだけ抑えたい一方で、セキュリティ基準の強化と権限境界の明確化は必須要件になっています。最も適切な方法はどれですか？

### 選択肢

**ア.** Cognitoユーザープールを使わず、すべてのパートナーに共通のIAMロールを発行してSIGv4署名でアクセスさせる

**イ.** APIキーを発行し、認証は相手側アプリケーションに委ねてAPI Gatewayではスロットリングだけを行う

**ウ.** Resource Policyで送信元IPを制限し、認証そのものはネットワーク制御中心で設計する

**エ.** LambdaオーソライザーまたはJWTオーソライザーを使い、外部IDプロバイダーのトークンをAPI Gatewayで検証する

<details>
<summary>解答と解説を表示</summary>

**正解:** エ. (詳細は各選択肢のポイントを参照)

#### 解説

外部IDプロバイダー連携の認証をAPI Gateway側で扱うには、LambdaまたはJWTオーソライザーが適しています。


#### 各選択肢のポイント


**ア.**

- 不正解。一時的認証情報による権限委任には使えますが、この設問で必要なJWT ベースの API 認証を直接満たせないため適していません。


**イ.**

- 不正解。一部の要件には対応できますが、この設問で必要なJWT ベースの API 認証を直接満たせないため適していません。


**ウ.**

- 不正解。リソース単位のアクセス制御には使えますが、この設問で必要なJWT ベースの API 認証を直接満たせないため適していません。


**エ.**

- 正解。外部IDプロバイダー連携の認証をAPI Gateway側で扱うには、LambdaまたはJWTオーソライザーが適しています。

</details>

---
