# セキュアなアーキテクチャの設計

AWS Solution Architect Associate 試験対策問題集

---

## 問題 15

インターネット非公開のRDSデータベースへ、同じVPC内のアプリケーションだけが接続できるようにしたい企業があります。OSレベルの管理アクセスも減らしたい前提で、加えて、セキュリティレビューでは、最小権限の維持、長期認証情報の削減、監査時に設定意図を説明しやすい構成であることも求められています。最も適切な設計はどれですか？

### 選択肢

**ア.** RDSを踏み台サーバーの背後へ置き、すべての接続をEC2経由のローカルポート転送へ統一する

**イ.** RDSをパブリックに公開し、IAM DB認証を使うことでネットワーク制御の必要を減らす

**ウ.** RDSをパブリックサブネットに配置し、セキュリティグループでアプリサーバーの送信元IPだけを許可する

**エ.** RDSをプライベートサブネットに配置し、アプリ層のセキュリティグループだけから接続を許可する

<details>
<summary>解答と解説を表示</summary>

**正解:** エ. RDSをプライベートサブネットに配置し、アプリ層のセキュリティグループだけから接続を許可する

#### 解説

RDSをインターネット非公開にし、アプリ層からだけ接続したいならプライベートサブネット配置とSG制御が適切です。


#### 各選択肢のポイント


**ア.**

- 不正解。一部の要件には対応できますが、この設問で必要なインスタンス単位の通信制御を直接満たせないため適していません。


**イ.**

- 不正解。一部の要件には対応できますが、この設問で必要なインスタンス単位の通信制御を直接満たせないため適していません。


**ウ.**

- 不正解。インスタンス単位の通信制御には使えますが、この設問で必要なインスタンス単位の通信制御を直接満たせないため適していません。


**エ.**

- 正解。RDSをインターネット非公開にし、アプリ層からだけ接続したいならプライベートサブネット配置とSG制御が適切です。

</details>

---

## 問題 16

パートナー企業向けにPrivate APIを提供したい企業があります。パブリックインターネットを経由させず、特定のVPCまたはアカウントからのみAPIへアクセスさせたい場合、加えて、セキュリティインシデント対応を見据えて、設定変更時の追跡性と継続的な運用のしやすさも重視されています。最も適切な方法はどれですか？

### 選択肢

**ア.** CloudFrontのオリジンにAPI Gatewayを設定し、キャッシュヒット率を上げることで外部公開の必要性を下げる

**イ.** Lambda URLを利用し、SIGv4署名を強制することでインターネット経由でも安全性を確保する

**ウ.** Regional APIとして公開し、WAFで送信元IP制限を行ってPrivate APIの代替とする

**エ.** API GatewayのPrivate APIを使い、VPCエンドポイントとリソースポリシーでアクセス元を制御する

<details>
<summary>解答と解説を表示</summary>

**正解:** エ. API GatewayのPrivate APIを使い、VPCエンドポイントとリソースポリシーでアクセス元を制御する

#### 解説

インターネットを経由させずに特定VPCなどからだけAPIを使わせたい場合は、API GatewayのPrivate APIとVPCエンドポイントが適しています。


#### 各選択肢のポイント


**ア.**

- 不正解。配信経路の追加ではなく、VPC 内からだけ到達できる設計が必要です。


**イ.**

- 不正解。公開エンドポイントであり、VPC 内限定アクセスの要件を満たせません。


**ウ.**

- 不正解。IP 制限はできますが、Private API のように VPC 内だけへ閉じた公開にはなりません。


**エ.**

- 正解。インターネットを経由させずに特定VPCなどからだけAPIを使わせたい場合は、API GatewayのPrivate APIとVPCエンドポイントが適しています。

</details>

---

## 問題 17

開発チームは、複数のLambda関数でデータベース認証情報を共有しています。認証情報の定期ローテーションが必要で、アプリケーションコードの変更は最小限にしたい状況です。加えて、セキュリティインシデント対応を見据えて、設定変更時の追跡性と継続的な運用のしやすさも重視されています。最も適切な方法はどれですか？

### 選択肢

**ア.** 認証情報をLambdaの環境変数に暗号化して保存し、ローテーション時は関数設定を更新して再デプロイする

**イ.** 認証情報をSecrets Managerに保存し、自動ローテーションを有効化してLambdaから必要時に取得する

**ウ.** 認証情報をParameter Storeの標準パラメータに保存し、ローテーションはEventBridgeで通知して運用担当者が手動更新する

**エ.** 認証情報をS3に保存してバケットを暗号化し、起動時にLambdaがオブジェクトを読み込む

<details>
<summary>解答と解説を表示</summary>

**正解:** イ. 認証情報をSecrets Managerに保存し、自動ローテーションを有効化してLambdaから必要時に取得する

#### 解説

シークレットの保管と自動ローテーションが必要なら、AWS Secrets Managerを使うのが最適です。


#### 各選択肢のポイント


**ア.**

- 不正解。関数設定の埋め込み値管理には使えますが、この設問で必要なシークレットの保管とローテーションを直接満たせないため適していません。


**イ.**

- 正解。シークレットの保管と自動ローテーションが必要なら、AWS Secrets Managerを使うのが最適です。


**ウ.**

- 不正解。手作業に依存し、設定漏れや運用負荷が増えやすいため適していません。


**エ.**

- 不正解。一部の要件には対応できますが、この設問で必要なシークレットの保管とローテーションを直接満たせないため適していません。

</details>

---

## 問題 18

顧客向けモバイルアプリのサインアップとサインインを実装したい企業があります。できるだけマネージドな認証基盤を使い、将来的にはソーシャルID連携も視野に入れています。最も適切なサービスはどれですか？また、監査部門からは、設定の一元管理とアクセス制御の明確化も求められており、運用チームは自前実装や手動運用をできるだけ増やしたくありません。

### 選択肢

**ア.** AWS Directory Serviceをインターネット向けに公開し、モバイル利用者の認証ストアとして使う

**イ.** IAMユーザーをアプリ利用者ごとに作成し、一時認証情報をモバイルアプリへ直接配布する

**ウ.** Systems Manager Session Managerを利用してアプリ利用者のログインセッションを管理する

**エ.** Amazon Cognitoを使い、ユーザープールで認証を管理し必要に応じてフェデレーションも利用する

<details>
<summary>解答と解説を表示</summary>

**正解:** エ. Amazon Cognitoを使い、ユーザープールで認証を管理し必要に応じてフェデレーションも利用する

#### 解説

一般利用者向けの認証基盤をマネージドで構築し、将来的なフェデレーションも考えるならAmazon Cognitoが適しています。


#### 各選択肢のポイント


**ア.**

- 不正解。社内ディレクトリ連携向けであり、一般利用者向けの認証基盤には適していません。


**イ.**

- 不正解。アプリ利用者ごとに IAM を配る設計は運用負荷が高く、一般利用者向けではありません。


**ウ.**

- 不正解。サーバー管理接続向けであり、アプリ利用者の認証には使えません。


**エ.**

- 正解。一般利用者向けの認証基盤をマネージドで構築し、将来的なフェデレーションも考えるならAmazon Cognitoが適しています。

</details>

---

## 問題 19

企業は、S3上の個人情報を含むデータへのアクセス状況を継続的に監視し、不審なアクセスパターンや機密データ露出の可能性を検知したいと考えています。最も適切なサービスの組み合わせはどれですか？また、監査部門からは、設定の一元管理とアクセス制御の明確化も求められており、運用チームは自前実装や手動運用をできるだけ増やしたくありません。

### 選択肢

**ア.** Trusted AdvisorでS3バケットのサイズ変化を確認し、増減が大きいときだけ個人情報の流出を調査する

**イ.** Macieで機密データの検出を行い、CloudTrailやEventBridgeと組み合わせて不審なアクセス状況を監視する

**ウ.** CodeGuru SecurityでS3オブジェクトの内容をスキャンし、アクセスログが多いオブジェクトを保護対象にする

**エ.** Compute OptimizerでS3の利用傾向を分析し、通常と違うアクセス頻度のバケットを自動隔離する

<details>
<summary>解答と解説を表示</summary>

**正解:** イ. Macieで機密データの検出を行い、CloudTrailやEventBridgeと組み合わせて不審なアクセス状況を監視する

#### 解説

機密データ検出にはMacieが適しており、アクセス監査やイベント通知と組み合わせると監視を強化できます。


#### 各選択肢のポイント


**ア.**

- 不正解。容量変化の確認では、機密データの有無や不審アクセスを継続監視できません。


**イ.**

- 正解。機密データ検出にはMacieが適しており、アクセス監査やイベント通知と組み合わせると監視を強化できます。


**ウ.**

- 不正解。コード分析向けであり、S3 データの機密性検出やアクセス監視には使えません。


**エ.**

- 不正解。コストやサイズ最適化向けであり、機密データ監視には不向きです。

</details>

---

## 問題 20

外部ベンダーが別AWSアカウントから特定のS3バケットへ定期的にデータを投入します。自社アカウント側では長期資格情報の払い出しを避け、必要最小限の権限でクロスアカウントアクセスさせたい場合、また、監査部門からは、設定の一元管理とアクセス制御の明確化も求められており、運用チームは自前実装や手動運用をできるだけ増やしたくありません。最も適切な方法はどれですか？

### 選択肢

**ア.** ベンダーアカウントが引き受けるクロスアカウントIAMロールを自社アカウントに作成し、対象バケットへの最小権限を付与する

**イ.** 自社アカウントでIAMユーザーを作成し、アクセスキーをベンダーへ渡してローテーションだけを月次で行う

**ウ.** ベンダーアカウントに自社のKMSキーだけ共有し、S3の書き込み権限はアプリケーション側の検証で補う

**エ.** S3バケットをパブリック書き込み可能にし、オブジェクトキーのプレフィックスでベンダーごとの書き込み範囲を分離する

<details>
<summary>解答と解説を表示</summary>

**正解:** ア. (詳細は各選択肢のポイントを参照)

#### 解説

クロスアカウントアクセスを安全に提供するには、相手側が引き受けるIAMロールを用意して最小権限を与えるのが適切です。


#### 各選択肢のポイント


**ア.**

- 正解。クロスアカウントアクセスを安全に提供するには、相手側が引き受けるIAMロールを用意して最小権限を与えるのが適切です。


**イ.**

- 不正解。長期認証情報の受け渡しが必要になり、管理負荷と漏えいリスクが増えます。


**ウ.**

- 不正解。暗号化キーの共有だけでは S3 への書き込み権限を安全に委任できません。


**エ.**

- 不正解。公開書き込みは過剰で、必要最小限のクロスアカウント権限付与になりません。

</details>

---

## 問題 21

企業は、複数のAWSアカウントにまたがるAmazon S3バケットの暗号化キーを一元管理したいと考えています。監査要件として、キー使用の追跡とキー無効化の制御が必要です。運用負荷を抑えながら要件を満たす。また、監査部門からは、設定の一元管理とアクセス制御の明確化も求められており、運用チームは自前実装や手動運用をできるだけ増やしたくありません。最も適切な設計はどれですか？

### 選択肢

**ア.** 各S3バケットでSSE-S3を有効化し、暗号化設定だけを統一して監査はCloudTrailのオブジェクトアクセス履歴で代替する

**イ.** S3バケットごとに別々のAWS管理キーを使い、監査レポートは月次で各アカウントから手動収集する

**ウ.** AWS KMSのカスタマーマネージドキーを利用し、必要に応じてクロスアカウント権限を付与してキー利用を一元管理する

**エ.** 機密データだけクライアント側暗号化を使い、復号鍵は各アプリケーションサーバーのローカルファイルで管理する

<details>
<summary>解答と解説を表示</summary>

**正解:** ウ. AWS KMSのカスタマーマネージドキーを利用し、必要に応じてクロスアカウント権限を付与してキー利用を一元管理する

#### 解説

キーの利用制御や監査、無効化を一元管理したい場合は、AWS KMSのカスタマーマネージドキーを使う設計が適しています。


#### 各選択肢のポイント


**ア.**

- 不正解。保存時暗号化はできますが、キー利用の監査やクロスアカウント管理の柔軟性が不足します。


**イ.**

- 不正解。保存時暗号化はできますが、キー利用の追跡や無効化を利用者側で細かく制御しにくいです。


**ウ.**

- 正解。キーの利用制御や監査、無効化を一元管理したい場合は、AWS KMSのカスタマーマネージドキーを使う設計が適しています。


**エ.**

- 不正解。方式としては可能ですが、鍵配布や監査が分散し、運用負荷が高くなります。

</details>

---

