# 高パフォーマンスなアーキテクチャの設計

AWS Solution Architect Associate 試験対策問題集

---

## 問題 8

アプリケーションは大量の小さなオブジェクトをS3へアップロードしています。世界中のクライアントからのアップロード時間を短縮したい場合、また、トラフィックは一定ではなく、短時間のスパイクも発生するため、性能要件を満たしつつ管理負荷を増やしすぎないことが重要です。最も適切な方法はどれですか？

### 選択肢

**ア.** Route 53のレイテンシールーティングを使って、各クライアントを最寄りのS3リージョンへ手動誘導する

**イ.** S3バケットをパブリックへ変更し、TLSハンドシェイクを省略して転送時間を短くする

**ウ.** CloudFrontをS3のアップロード専用オリジンとして構成し、PUTリクエストをすべてキャッシュする

**エ.** S3 Transfer Accelerationを有効化し、エッジロケーション経由で最適なAWSネットワークへ取り込む

<details>
<summary>解答と解説を表示</summary>

**正解:** エ. S3 Transfer Accelerationを有効化し、エッジロケーション経由で最適なAWSネットワークへ取り込む

#### 解説

世界中からのS3アップロードを高速化したいなら、S3 Transfer Accelerationが有効です。


#### 各選択肢のポイント


**ア.**

- 不正解。手作業に依存し、設定漏れや運用負荷が増えやすいため適していません。


**イ.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**ウ.**

- 不正解。コンテンツ配信とキャッシュには使えますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**エ.**

- 正解。世界中からのS3アップロードを高速化したいなら、S3 Transfer Accelerationが有効です。

</details>

---

## 問題 9

分析チームは、ペタバイト規模のデータに対して標準SQLで高速分析したいと考えています。クエリ結果は数秒〜数十秒で返り、列指向保存と並列処理の恩恵を受けたい場合、最も適切なサービスはどれですか？また、トラフィックは一定ではなく、短時間のスパイクも発生するため、性能要件を満たしつつ管理負荷を増やしすぎないことが重要です。

### 選択肢

**ア.** Amazon DynamoDBを使い、各分析クエリをScanで実行して低レイテンシを狙う

**イ.** Amazon Athenaを使い、必要に応じてParquet化したS3データへ直接SQLを発行する

**ウ.** Amazon Redshiftを使い、データウェアハウスとして列指向・並列処理で大規模分析を行う

**エ.** Amazon RDS for PostgreSQLを単一インスタンスで使い、パーティション分割だけでペタバイト分析へ対応する

<details>
<summary>解答と解説を表示</summary>

**正解:** ウ. Amazon Redshiftを使い、データウェアハウスとして列指向・並列処理で大規模分析を行う

#### 解説

ペタバイト規模の高速分析には、列指向で並列処理が可能なRedshiftが適しています。


#### 各選択肢のポイント


**ア.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**イ.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**ウ.**

- 正解。ペタバイト規模の高速分析には、列指向で並列処理が可能なRedshiftが適しています。


**エ.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。

</details>

---

## 問題 10

グローバルWebアプリケーションでは、動的コンテンツも含めてネットワーク経路を最適化したいと考えています。TCP接続確立やインターネットの経路品質の影響をできるだけ減らしたい場合、最も適切なサービスはどれですか？加えて、体感性能の悪化はすぐにビジネス影響へつながるため、レイテンシ、スケーラビリティ、実装変更量を総合的に比較して判断する必要があります。

### 選択肢

**ア.** S3 Transfer Accelerationを使い、動的Webリクエストも同じ仕組みで高速化する

**イ.** CloudFormationでALBの数を増やし、各リージョンに均等配分するだけで経路品質も改善する

**ウ.** Route 53の加重ルーティングを使い、重みを頻繁に変えることで経路品質を最適化する

**エ.** Global Acceleratorを使い、AWSグローバルネットワークへ早く取り込んで最適なアプリケーションエンドポイントへ送る

<details>
<summary>解答と解説を表示</summary>

**正解:** エ. Global Acceleratorを使い、AWSグローバルネットワークへ早く取り込んで最適なアプリケーションエンドポイントへ送る

#### 解説

動的トラフィックの経路最適化にはGlobal Acceleratorが適しています。


#### 各選択肢のポイント


**ア.**

- 不正解。一部の要件には対応できますが、この設問で必要なグローバルなトラフィック最適化を直接満たせないため適していません。


**イ.**

- 不正解。HTTP/HTTPS の負荷分散には使えますが、この設問で必要なグローバルなトラフィック最適化を直接満たせないため適していません。


**ウ.**

- 不正解。DNS ルーティングやヘルスチェックには使えますが、この設問で必要なグローバルなトラフィック最適化を直接満たせないため適していません。


**エ.**

- 正解。動的トラフィックの経路最適化にはGlobal Acceleratorが適しています。

</details>

---

## 問題 11

同じAZ内の多数のEC2インスタンス間で低レイテンシかつ高スループットのネットワーク通信が必要な分散処理があります。ネットワーク性能を最優先したい場合、また、最近は利用者数の増加とアクセスの偏りが目立っており、ピーク時間帯でも性能を維持しながら、大規模なリファクタリングは避けたいという制約があります。最も適切な設計はどれですか？

### 選択肢

**ア.** クラスター配置グループを利用し、関連インスタンスを近接配置してネットワーク性能を高める

**イ.** スプレッド配置グループを使い、インスタンスをできるだけ離して配置して帯域競合を防ぐ

**ウ.** パーティション配置グループを使い、各インスタンスを別AZに分散させて通信距離を短縮する

**エ.** Placement Groupは使わず、インスタンスタイプだけを大きくしてネットワーク性能を補う

<details>
<summary>解答と解説を表示</summary>

**正解:** ア. (詳細は各選択肢のポイントを参照)

#### 解説

低レイテンシと高スループットの東西通信には、クラスター配置グループが有効です。


#### 各選択肢のポイント


**ア.**

- 正解。低レイテンシと高スループットの東西通信には、クラスター配置グループが有効です。


**イ.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**ウ.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**エ.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。

</details>

---

## 問題 12

メディア企業は、複数ビットレートの動画を世界中へ配信しています。再生開始を速くし、視聴者のネットワーク状況に応じて配信品質を最適化したい場合、加えて、体感性能の悪化はすぐにビジネス影響へつながるため、レイテンシ、スケーラビリティ、実装変更量を総合的に比較して判断する必要があります。最も適切な方法はどれですか？

### 選択肢

**ア.** ALBで動画ファイルを直接配信し、オリジンサーバー側のCPU性能を上げて世界配信へ対応する

**イ.** Route 53の重み付けルーティングで解像度別のURLを返し、クライアントが最速のものを推測する

**ウ.** S3の静的ウェブサイト機能だけを使い、視聴者に手動で適切な解像度ファイルを選ばせる

**エ.** CloudFrontと適切な動画配信構成を利用し、エッジ配信で開始遅延を減らしつつアダプティブ配信を行う

<details>
<summary>解答と解説を表示</summary>

**正解:** エ. CloudFrontと適切な動画配信構成を利用し、エッジ配信で開始遅延を減らしつつアダプティブ配信を行う

#### 解説

動画のグローバル配信には、CloudFrontを中心にしたアダプティブ配信が適しています。


#### 各選択肢のポイント


**ア.**

- 不正解。HTTP/HTTPS の負荷分散には使えますが、この設問で必要なコンテンツ配信とキャッシュを直接満たせないため適していません。


**イ.**

- 不正解。DNS ルーティングやヘルスチェックには使えますが、この設問で必要なコンテンツ配信とキャッシュを直接満たせないため適していません。


**ウ.**

- 不正解。手作業に依存し、設定漏れや運用負荷が増えやすいため適していません。


**エ.**

- 正解。動画のグローバル配信には、CloudFrontを中心にしたアダプティブ配信が適しています。

</details>

---

## 問題 13

レイテンシに敏感なTCP/UDPアプリケーションを世界中の利用者へ提供しています。複数リージョンのALBへトラフィックを最適経路で送信し、Anycast IPも使いたい場合、最も適切なサービスはどれですか？また、最近は利用者数の増加とアクセスの偏りが目立っており、ピーク時間帯でも性能を維持しながら、大規模なリファクタリングは避けたいという制約があります。

### 選択肢

**ア.** CloudFrontを使い、すべてのTCP/UDPトラフィックをエッジロケーションから中継する

**イ.** Route 53のレイテンシールーティングだけを使い、TCPトラフィックもDNSで最適化する

**ウ.** Transit Gatewayをグローバルに展開し、インターネット利用者から直接接続させる

**エ.** AWS Global Acceleratorを使い、Anycast IP経由で最適なリージョンのエンドポイントへ送る

<details>
<summary>解答と解説を表示</summary>

**正解:** エ. AWS Global Acceleratorを使い、Anycast IP経由で最適なリージョンのエンドポイントへ送る

#### 解説

TCP/UDPアプリケーションをグローバルに低レイテンシで提供するには、Global Acceleratorが適しています。


#### 各選択肢のポイント


**ア.**

- 不正解。コンテンツ配信とキャッシュには使えますが、この設問で必要なグローバルなトラフィック最適化を直接満たせないため適していません。


**イ.**

- 不正解。DNS ルーティングやヘルスチェックには使えますが、この設問で必要なグローバルなトラフィック最適化を直接満たせないため適していません。


**ウ.**

- 不正解。VPC やネットワーク間接続の集約には使えますが、この設問で必要なグローバルなトラフィック最適化を直接満たせないため適していません。


**エ.**

- 正解。TCP/UDPアプリケーションをグローバルに低レイテンシで提供するには、Global Acceleratorが適しています。

</details>

---

## 問題 14

大規模な並列I/Oが必要なHPCワークロードでは、数千のEC2インスタンスから共有ファイルシステムへ高スループットでアクセスする必要があります。最も適切なストレージはどれですか？また、トラフィックは一定ではなく、短時間のスパイクも発生するため、性能要件を満たしつつ管理負荷を増やしすぎないことが重要です。

### 選択肢

**ア.** Amazon FSx for Lustreを使い、高性能な並列ファイルシステムとしてワークロードへ提供する

**イ.** Amazon S3を直接POSIXファイルシステムとしてマウントし、すべてのノードが同時書き込みする

**ウ.** Amazon EFSを使い、一般的なNFS共有としてすべてのHPCノードから利用する

**エ.** Amazon EBSを1台のEC2へアタッチし、そのEC2をNFSサーバーとして他ノードへ共有する

<details>
<summary>解答と解説を表示</summary>

**正解:** ア. (詳細は各選択肢のポイントを参照)

#### 解説

HPCの高スループット共有ストレージには、FSx for Lustreが適しています。


#### 各選択肢のポイント


**ア.**

- 正解。HPCの高スループット共有ストレージには、FSx for Lustreが適しています。


**イ.**

- 不正解。一部の要件には対応できますが、この設問で必要な高性能共有ファイルシステムを直接満たせないため適していません。


**ウ.**

- 不正解。共有ファイルストレージには使えますが、この設問で必要な高性能共有ファイルシステムを直接満たせないため適していません。


**エ.**

- 不正解。一部の要件には対応できますが、この設問で必要な高性能共有ファイルシステムを直接満たせないため適していません。

</details>

---

