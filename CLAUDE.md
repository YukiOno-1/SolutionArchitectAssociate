# AWS資格試験対策リポジトリ

## 目的

AWS認定資格の過去問・想定問題をカテゴリ別にMarkdownで管理し、
学習しやすい単位に分割して保存するリポジトリ。

## ファイル構成のルール

### カテゴリファイル

出題範囲のカテゴリごとに1ファイル作成する。
ファイル名は先頭にカテゴリ番号を付け、続けてカテゴリ名を記載する。

```
<カテゴリ番号><カテゴリ名>.md
```

例: `1セキュアなアーキテクチャの設計.md`

カテゴリ番号は資格試験の出題分野の並び順に割り当てる。

### 問題フォーマット

各問題は以下の構造に統一する。

```
## 問題 N

問題文

### 選択肢

**ア.** 選択肢1
**イ.** 選択肢2
**ウ.** 選択肢3
**エ.** 選択肢4

**解答と解説を表示**

**正解:** ...

#### 解説

解説文

#### 各選択肢のポイント

**ア.**
- 正解/不正解の理由

**イ.**
- 正解/不正解の理由

（以下同様）

---
```

問題の区切りは `## 問題 N` の見出しで判定する。

### 分割ファイル

1カテゴリの問題数が多い場合、7問ずつのファイルに分割する。
分割後のファイル名は次の形式にする。

```
<カテゴリ番号><カテゴリ名><カテゴリ番号>-<パート番号>.md
```

例: `1セキュアなアーキテクチャの設計1-1.md`（カテゴリ1のパート1）

## 新しい資格のリポジトリを作る際の手順

1. リポジトリを新規作成する（資格名をリポジトリ名にする）
2. 出題分野ごとにカテゴリ番号を割り当て、カテゴリファイルを作成する
3. 各カテゴリファイルに問題を上記フォーマットで追加する
4. 問題数が多いカテゴリは7問ずつのファイルに分割する
