# 弾力性の高いアーキテクチャの設計

AWS Solution Architect Associate 試験対策問題集

---

## 問題 8

複数のEC2インスタンスから構成されるアプリケーションは、起動後にセッション情報をローカルメモリへ保持しています。インスタンス障害やスケールインがあってもユーザーセッションを維持しやすくしたい場合、最も適切な改善はどれですか？さらに、障害時の影響を局所化し、復旧にかかる手順をできるだけ標準化したいと考えており、追加の運用負荷は最小限に抑える必要があります。

### 選択肢

**ア.** ALBのスティッキーセッションを長時間有効化し、各ユーザーを可能な限り同じEC2へ固定する

**イ.** セッション情報をElastiCache for Redisなどの外部共有ストアへ移し、アプリサーバーをステートレス化する

**ウ.** 各インスタンスのEBSボリュームへセッションを書き出し、障害時は別インスタンスへEBSを付け替える

**エ.** Auto Scalingグループの最小台数を1台にして、スケールインによるセッション消失を防ぐ

<details>
<summary>解答と解説を表示</summary>

**正解:** イ. セッション情報をElastiCache for Redisなどの外部共有ストアへ移し、アプリサーバーをステートレス化する

#### 解説

可用性を高めるには、アプリサーバーをステートレスにしてセッションを共有ストアへ外出しするのが適切です。


#### 各選択肢のポイント


**ア.**

- 不正解。HTTP/HTTPS の負荷分散には使えますが、この設問で必要な共有キャッシュやセッション保管を直接満たせないため適していません。


**イ.**

- 正解。可用性を高めるには、アプリサーバーをステートレスにしてセッションを共有ストアへ外出しするのが適切です。


**ウ.**

- 不正解。一部の要件には対応できますが、この設問で必要な共有キャッシュやセッション保管を直接満たせないため適していません。


**エ.**

- 不正解。負荷に応じた台数調整には使えますが、この設問で必要な共有キャッシュやセッション保管を直接満たせないため適していません。

</details>

---

## 問題 9

企業は、注文イベントを複数の下流システムへ配信したいと考えています。会計、通知、分析の各システムは独立して処理し、ある購読先の障害が他に影響しないようにしたい場合、最も適切な構成はどれですか？さらに、障害時の影響を局所化し、復旧にかかる手順をできるだけ標準化したいと考えており、追加の運用負荷は最小限に抑える必要があります。

### 選択肢

**ア.** SNSトピックへイベントを発行し、各下流システムは個別のSQSキューやエンドポイントで購読する

**イ.** DynamoDB Streamsを直接3つのシステムへ接続し、同一ストリームから並列に読み取らせる

**ウ.** EventBridgeを使わず、注文APIから会計、通知、分析の各エンドポイントへ順次同期呼び出しする

**エ.** 1つのSQSキューへすべての下流システムを接続し、各システムが必要なメッセージだけを読み飛ばす

<details>
<summary>解答と解説を表示</summary>

**正解:** ア. (詳細は各選択肢のポイントを参照)

#### 解説

複数の独立購読先へ疎結合に配信し、障害の影響を分離するにはSNSと個別購読先の組み合わせが適しています。


#### 各選択肢のポイント


**ア.**

- 正解。複数の独立購読先へ疎結合に配信し、障害の影響を分離するにはSNSと個別購読先の組み合わせが適しています。


**イ.**

- 不正解。DynamoDB 変更イベントの取得には使えますが、この設問で必要な疎結合なキューイングを直接満たせないため適していません。


**ウ.**

- 不正解。イベントルーティングには使えますが、この設問で必要な疎結合なキューイングを直接満たせないため適していません。


**エ.**

- 不正解。疎結合なキューイングには使えますが、この設問で必要な疎結合なキューイングを直接満たせないため適していません。

</details>

---

## 問題 10

オンプレミスからAWSへバックアップを複製しています。ランサムウェアや誤削除に備え、バックアップ自体の保護も強めたい場合、最も適切な考え方はどれですか？さらに、障害時の影響を局所化し、復旧にかかる手順をできるだけ標準化したいと考えており、追加の運用負荷は最小限に抑える必要があります。

### 選択肢

**ア.** Amazon EFSへバックアップを集約し、NFSアクセス制御で上書きを防止する

**イ.** バックアップがあること自体が保護になるため、保持期間を短くして毎日新しいバックアップへ置き換える

**ウ.** すべてのバックアップを同一アカウントの単一S3バケットへ保管し、削除は管理者のみ許可する

**エ.** AWS Backupのバックアップボールトを使い、必要に応じてVault Lockやクロスアカウントコピーを組み合わせる

<details>
<summary>解答と解説を表示</summary>

**正解:** エ. AWS Backupのバックアップボールトを使い、必要に応じてVault Lockやクロスアカウントコピーを組み合わせる

#### 解説

バックアップ自体の保護を強めるには、AWS Backupの機能やVault Lock、別アカウントコピーが有効です。


#### 各選択肢のポイント


**ア.**

- 不正解。共有ファイルストレージには使えますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**イ.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**ウ.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**エ.**

- 正解。バックアップ自体の保護を強めるには、AWS Backupの機能やVault Lock、別アカウントコピーが有効です。

</details>

---

## 問題 11

クライアントからの注文APIは、短時間に同じリクエストが複数回送られる可能性があります。ネットワーク障害時の再送でも重複注文を防ぎたい場合、また、現在の運用手順は手作業が多く、担当者依存を減らしながら障害耐性を高めたいという要望があります。最も適切な設計はどれですか？

### 選択肢

**ア.** クライアントごとに固定の接続を維持し、再送が起きないようTCPセッションを長寿命化する

**イ.** API Gatewayのスロットリングだけを設定し、同時リクエスト数を減らして重複登録を防ぐ

**ウ.** 注文作成APIに冪等キーを導入し、同じ要求識別子に対しては同一結果を返すようにする

**エ.** 重複は後続の会計処理で見つけて取消す前提とし、受付APIでは毎回新規注文として登録する

<details>
<summary>解答と解説を表示</summary>

**正解:** ウ. 注文作成APIに冪等キーを導入し、同じ要求識別子に対しては同一結果を返すようにする

#### 解説

再送や重複送信に備えるには、冪等キーを使って同じ要求を一意に扱う設計が適切です。


#### 各選択肢のポイント


**ア.**

- 不正解。一部の要件には対応できますが、この設問で必要な重複実行時も結果を一貫させる設計を直接満たせないため適していません。


**イ.**

- 不正解。一部の要件には対応できますが、この設問で必要な重複実行時も結果を一貫させる設計を直接満たせないため適していません。


**ウ.**

- 正解。再送や重複送信に備えるには、冪等キーを使って同じ要求を一意に扱う設計が適切です。


**エ.**

- 不正解。一部の要件には対応できますが、この設問で必要な重複実行時も結果を一貫させる設計を直接満たせないため適していません。

</details>

---

## 問題 12

企業は、マイクロサービス間のイベント連携を行っています。各サービスは独立しており、将来新しい購読先が追加されても既存送信側の変更を減らしたい場合、また、同社は、単一障害点を避けつつ、障害発生時でもサービス継続性を高めたいと考えていますが、運用チームの人数は限られているため複雑な自前運用は避けたい状況です。最も適切な設計はどれですか？

### 選択肢

**ア.** 各マイクロサービスへ共通ライブラリを配布し、そのライブラリ内で受信先一覧を固定化して運用する

**イ.** イベント内容を共有データベースへ書き込み、各サービスが同じテーブルをポーリングして受信する

**ウ.** イベントバスまたはトピックを中心にしたパブリッシュ/サブスクライブ設計へし、送信側はイベント発行だけを行う

**エ.** 送信側サービスからすべての購読先APIを順次呼び出す設計にし、追加先が増えたら送信側ロジックへ組み込む

<details>
<summary>解答と解説を表示</summary>

**正解:** ウ. イベントバスまたはトピックを中心にしたパブリッシュ/サブスクライブ設計へし、送信側はイベント発行だけを行う

#### 解説

疎結合で購読先追加にも強いのは、イベントバスやトピックを中心としたPub/Sub設計です。


#### 各選択肢のポイント


**ア.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**イ.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**ウ.**

- 正解。疎結合で購読先追加にも強いのは、イベントバスやトピックを中心としたPub/Sub設計です。


**エ.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。

</details>

---

## 問題 13

オブジェクトストレージに保存された重要ファイルについて、リージョン障害が発生しても別リージョンで復旧できるようにしたい企業があります。更新頻度は低く、RPOを小さく保ちたい場合、また、現在の運用手順は手作業が多く、担当者依存を減らしながら障害耐性を高めたいという要望があります。最も適切な方法はどれですか？

### 選択肢

**ア.** S3バージョニングだけを有効化し、リージョン障害時は同じバケットから過去版を読み出す

**イ.** S3クロスリージョンレプリケーションを設定し、別リージョンへ継続複製する

**ウ.** EBSスナップショットを取得して別リージョンのEC2へアタッチできるよう準備する

**エ.** CloudFrontでオブジェクトを長期間キャッシュし、元リージョン障害時はエッジキャッシュを恒久データソースとして扱う

<details>
<summary>解答と解説を表示</summary>

**正解:** イ. S3クロスリージョンレプリケーションを設定し、別リージョンへ継続複製する

#### 解説

リージョン障害に備えてS3データを別リージョンへ継続的に複製するには、CRRが適しています。


#### 各選択肢のポイント


**ア.**

- 不正解。削除や上書きからの復旧性向上には使えますが、この設問で必要なS3 オブジェクトの別リージョン複製を直接満たせないため適していません。


**イ.**

- 正解。リージョン障害に備えてS3データを別リージョンへ継続的に複製するには、CRRが適しています。


**ウ.**

- 不正解。ブロックストレージのバックアップには使えますが、この設問で必要なS3 オブジェクトの別リージョン複製を直接満たせないため適していません。


**エ.**

- 不正解。コンテンツ配信とキャッシュには使えますが、この設問で必要なS3 オブジェクトの別リージョン複製を直接満たせないため適していません。

</details>

---

## 問題 14

多層アプリケーションでは、Web層、アプリ層、DB層がそれぞれ別サブネットにあります。単一のAZ障害が発生してもサービス継続したい場合、最も適切なサブネット設計はどれですか？加えて、将来的なトラフィック増加も見込まれているため、可用性だけでなく拡張性と運用のしやすさも同時に満たす設計が求められています。

### 選択肢

**ア.** 各層のサブネットを1つのAZにだけ作成し、障害時は別AZへ手動で新しいサブネットを作る

**イ.** DB層だけ複数AZにし、Web層とアプリ層はAuto Scalingで補完する設計に寄せる

**ウ.** Web層だけ複数AZに分散し、アプリ層とDB層は運用簡素化のため単一AZへ集約する

**エ.** 各層のサブネットを少なくとも2つのAZに分散して配置し、依存サービスもマルチAZで構成する

<details>
<summary>解答と解説を表示</summary>

**正解:** エ. 各層のサブネットを少なくとも2つのAZに分散して配置し、依存サービスもマルチAZで構成する

#### 解説

AZ障害への耐性を持たせるには、各層を少なくとも2つのAZへ分散配置するのが基本です。


#### 各選択肢のポイント


**ア.**

- 不正解。手作業に依存し、設定漏れや運用負荷が増えやすいため適していません。


**イ.**

- 不正解。負荷に応じた台数調整には使えますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**ウ.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**エ.**

- 正解。AZ障害への耐性を持たせるには、各層を少なくとも2つのAZへ分散配置するのが基本です。

</details>

---

