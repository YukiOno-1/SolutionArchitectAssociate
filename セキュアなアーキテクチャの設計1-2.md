# セキュアなアーキテクチャの設計

AWS Solution Architect Associate 試験対策問題集

---

## 問題 8

動画配信会社はAmazon CloudFront経由でプライベートなS3コンテンツを配信しています。ユーザーにはCloudFront経由だけでアクセスさせ、S3バケットへの直接アクセスは防ぎたい状況です。最新の推奨設計として、さらに、既存アプリケーションへの影響はできるだけ抑えたい一方で、セキュリティ基準の強化と権限境界の明確化は必須要件になっています。最も適切なのはどれですか？

### 選択肢

**ア.** CloudFrontのキャッシュポリシーでAuthorizationヘッダーを転送しないようにして、S3への直接アクセスを事実上抑止する

**イ.** S3静的ウェブサイトホスティングを有効化し、Refererヘッダーを検査してCloudFront以外のアクセスを拒否する

**ウ.** S3バケットをパブリックにして、CloudFrontでは署名付きURLだけを強制する

**エ.** CloudFrontのオリジンアクセスコントロールを使い、S3バケットポリシーでCloudFrontからのアクセスだけを許可する

<details>
<summary>解答と解説を表示</summary>

**正解:** エ. CloudFrontのオリジンアクセスコントロールを使い、S3バケットポリシーでCloudFrontからのアクセスだけを許可する

#### 解説

CloudFront経由だけでS3へアクセスさせたい場合は、OACを使ってS3バケットポリシーでCloudFrontのみ許可するのが推奨です。


#### 各選択肢のポイント


**ア.**

- 不正解。ヘッダー転送設定では S3 への直接アクセスそのものを防げません。


**イ.**

- 不正解。Referer による判定は信頼できるアクセス制御になりません。


**ウ.**

- 不正解。S3 を公開すると、CloudFront 経由だけに限定する要件を満たせません。


**エ.**

- 正解。CloudFront経由だけでS3へアクセスさせたい場合は、OACを使ってS3バケットポリシーでCloudFrontのみ許可するのが推奨です。

</details>

---

## 問題 9

組織は、特定のタグが付いたEC2インスタンスにだけSSH接続を許可したい一方、インバウンドポートの開放は極力減らしたいと考えています。加えて、セキュリティインシデント対応を見据えて、設定変更時の追跡性と継続的な運用のしやすさも重視されています。最も適切な方法はどれですか？

### 選択肢

**ア.** セキュリティグループで22番ポートを社内IPだけへ開け、タグ条件は運用手順で補完する

**イ.** Session Managerを使ってインバウンドポートを開けずに接続し、IAMポリシーでタグベースのアクセス制御を行う

**ウ.** Client VPNを導入し、接続後は各EC2インスタンスへOSユーザーのローカルパスワードでSSHする

**エ.** Systems Manager Run Commandで対話型シェルを代替し、特定タグのインスタンスへだけ一括コマンド実行する

<details>
<summary>解答と解説を表示</summary>

**正解:** イ. (詳細は各選択肢のポイントを参照)

#### 解説

SSHポートを開けずにタグ条件でアクセス制御したいなら、Session ManagerとタグベースIAM制御の組み合わせが適しています。


#### 各選択肢のポイント


**ア.**

- 不正解。SSH ポートの開放が残り、インバウンドを極力減らしたい要件に合いません。


**イ.**

- 正解。SSHポートを開けずにタグ条件でアクセス制御したいなら、Session ManagerとタグベースIAM制御の組み合わせが適しています。


**ウ.**

- 不正解。接続はできますが、タグベースで細かく管理しつつポート開放を避ける設計にはなりません。


**エ.**

- 不正解。一括実行には向きますが、対話的な接続をタグ条件で制御する用途には適していません。

</details>

---

## 問題 10

公開WebサイトはCloudFrontとALBの背後で運用されています。アプリケーションは利用者がアップロードしたファイルをS3へ直接保存しますが、アップロード先は自社オリジンだけに限定し、クライアントへAWS認証情報を配布したくありません。また、監査部門からは、設定の一元管理とアクセス制御の明確化も求められており、運用チームは自前実装や手動運用をできるだけ増やしたくありません。最も適切な方法はどれですか？

### 選択肢

**ア.** S3バケットをパブリック書き込み可能にして、アップロード後にLambdaで不正ファイルを削除する

**イ.** CloudFrontのPUTメソッドを有効にし、S3バケット側は認証なしでオブジェクト作成を受け付ける

**ウ.** アプリケーションが必要に応じてS3の事前署名URLを発行し、クライアントはそのURL経由で直接アップロードする

**エ.** クライアントへIAMユーザーを払い出し、アップロード専用の最小権限アクセスキーを配布する

<details>
<summary>解答と解説を表示</summary>

**正解:** ウ. アプリケーションが必要に応じてS3の事前署名URLを発行し、クライアントはそのURL経由で直接アップロードする

#### 解説

クライアントへ資格情報を配らずにS3へ直接アップロードさせるには、事前署名URLの利用が適しています。


#### 各選択肢のポイント


**ア.**

- 不正解。一部の要件には対応できますが、この設問で必要な限定的なS3アップロードやダウンロードの委任を直接満たせないため適していません。


**イ.**

- 不正解。コンテンツ配信とキャッシュには使えますが、この設問で必要な限定的なS3アップロードやダウンロードの委任を直接満たせないため適していません。


**ウ.**

- 正解。クライアントへ資格情報を配らずにS3へ直接アップロードさせるには、事前署名URLの利用が適しています。


**エ.**

- 不正解。長期的または固定的な認証情報の管理が残り、漏えいリスクと運用負荷が増えるため適していません。

</details>

---

## 問題 11

企業は、VPCフローログやCloudTrailログを中央アカウントへ集約しています。保存先バケットはログ配送サービスと監査担当だけが利用できるようにしたい場合、加えて、セキュリティインシデント対応を見据えて、設定変更時の追跡性と継続的な運用のしやすさも重視されています。最も適切な方法はどれですか？

### 選択肢

**ア.** サービスプリンシパルや集約ロールだけに必要な書き込み権限を与え、監査担当には読み取り専用権限を付与する

**イ.** ログ配送はKinesis Data Firehoseへ統一し、バケットポリシーでは配送元アカウントの全ユーザー書き込みを許可する

**ウ.** 保存先バケットはパブリックアクセスを禁止した上で、アカウント内のすべてのIAMエンティティに読み取り権限を与える

**エ.** ログ保存先バケットへ広めの管理者権限を付与し、監査時だけ人手でアクセス元を確認する

<details>
<summary>解答と解説を表示</summary>

**正解:** ア. (詳細は各選択肢のポイントを参照)

#### 解説

中央集約バケットは、書き込み元サービスや集約ロール、読み取り側監査担当に必要最小限の権限だけを与えるべきです。


#### 各選択肢のポイント


**ア.**

- 正解。中央集約バケットは、書き込み元サービスや集約ロール、読み取り側監査担当に必要最小限の権限だけを与えるべきです。


**イ.**

- 不正解。ストリーム収集には使えますが、この設問で必要なAWS サービスからのアクセス許可を直接満たせないため適していません。


**ウ.**

- 不正解。一部の要件には対応できますが、この設問で必要なAWS サービスからのアクセス許可を直接満たせないため適していません。


**エ.**

- 不正解。手作業に依存し、設定漏れや運用負荷が増えやすいため適していません。

</details>

---

## 問題 12

企業は、オンプレミスのActive Directoryを使ってAWSマネジメントコンソールへシングルサインオンしたいと考えています。複数AWSアカウントへの権限割り当ても一元管理したい場合、最も適切なサービスの組み合わせはどれですか？加えて、セキュリティレビューでは、最小権限の維持、長期認証情報の削減、監査時に設定意図を説明しやすい構成であることも求められています。

### 選択肢

**ア.** Directory Serviceだけを導入し、各AWSアカウントのコンソール権限はローカルIAMグループで個別管理する

**イ.** 各AWSアカウントに同一のIAMユーザーを作成し、フェデレーションなしでパスワードを同期する

**ウ.** Amazon Cognitoを管理者サインインにも使い、各アカウントの管理権限はアプリケーションロールで代替する

**エ.** AWS IAM Identity Centerを利用し、外部IDプロバイダーや既存ディレクトリと連携してアカウントアクセスを管理する

<details>
<summary>解答と解説を表示</summary>

**正解:** エ. AWS IAM Identity Centerを利用し、外部IDプロバイダーや既存ディレクトリと連携してアカウントアクセスを管理する

#### 解説

複数アカウントへのSSOと集中管理には、IAM Identity Centerの利用が適しています。


#### 各選択肢のポイント


**ア.**

- 不正解。一部の要件には対応できますが、この設問で必要な複数アカウント向けの SSO と権限集中管理を直接満たせないため適していません。


**イ.**

- 不正解。長期的または固定的な認証情報の管理が残り、漏えいリスクと運用負荷が増えるため適していません。


**ウ.**

- 不正解。アプリ利用者向けの認証基盤には使えますが、この設問で必要な複数アカウント向けの SSO と権限集中管理を直接満たせないため適していません。


**エ.**

- 正解。複数アカウントへのSSOと集中管理には、IAM Identity Centerの利用が適しています。

</details>

---

## 問題 13

企業は、各アカウントで自動作成されるセキュリティグループに過剰なルールが追加されないようにしたいと考えています。違反検知だけでなく、ルール逸脱を継続的に可視化したい場合、加えて、セキュリティレビューでは、最小権限の維持、長期認証情報の削減、監査時に設定意図を説明しやすい構成であることも求められています。最も適切な設計はどれですか？

### 選択肢

**ア.** AWS ConfigのマネージドルールやConformance Packを使ってセキュリティグループ設定の準拠状況を継続評価する

**イ.** CloudTrailだけを有効化し、CreateSecurityGroupとAuthorizeSecurityGroupIngressイベントを手動で追跡する

**ウ.** Trusted Advisorの結果を週次でエクスポートし、ルール違反の検知と可視化をすべて代替する

**エ.** VPC Flow Logsを使って意図しない通信を見つけたら、その都度セキュリティグループ設定を逆引きして確認する

<details>
<summary>解答と解説を表示</summary>

**正解:** ア. (詳細は各選択肢のポイントを参照)

#### 解説

セキュリティグループの設定逸脱を継続評価して可視化するには、AWS ConfigのルールやConformance Packが適しています。


#### 各選択肢のポイント


**ア.**

- 正解。セキュリティグループの設定逸脱を継続評価して可視化するには、AWS ConfigのルールやConformance Packが適しています。


**イ.**

- 不正解。手作業に依存し、設定漏れや運用負荷が増えやすいため適していません。


**ウ.**

- 不正解。手作業に依存し、設定漏れや運用負荷が増えやすいため適していません。


**エ.**

- 不正解。手作業に依存し、設定漏れや運用負荷が増えやすいため適していません。

</details>

---

## 問題 14

コンプライアンス要件により、すべてのAWSアカウントでAPI操作の証跡を一元保管し、改ざん耐性のある形で長期保存する必要があります。加えて、セキュリティインシデント対応を見据えて、設定変更時の追跡性と継続的な運用のしやすさも重視されています。最も適切な方法はどれですか？

### 選択肢

**ア.** Organization trailを設定してCloudTrailログを集中S3バケットへ集約し、ログファイル検証も有効化する

**イ.** EventBridgeですべてのAPIイベントを収集し、DynamoDBテーブルへ逐次保存して監査証跡とする

**ウ.** Configルールで変更履歴を記録し、S3への定期エクスポートでAPI証跡の代わりに使う

**エ.** 各アカウントでCloudWatch Logsへ個別出力し、必要な期間だけログ保持を延長する

<details>
<summary>解答と解説を表示</summary>

**正解:** ア. (詳細は各選択肢のポイントを参照)

#### 解説

複数アカウントのAPI操作証跡を一元管理し改ざん検知も行うには、Organization trailとログファイル検証付きCloudTrailが適しています。


#### 各選択肢のポイント


**ア.**

- 正解。複数アカウントのAPI操作証跡を一元管理し改ざん検知も行うには、Organization trailとログファイル検証付きCloudTrailが適しています。


**イ.**

- 不正解。イベントルーティングには使えますが、この設問で必要なAPI 操作の証跡取得を直接満たせないため適していません。


**ウ.**

- 不正解。一部の要件には対応できますが、この設問で必要なAPI 操作の証跡取得を直接満たせないため適していません。


**エ.**

- 不正解。ログ収集には使えますが、この設問で必要なAPI 操作の証跡取得を直接満たせないため適していません。

</details>

---

