# 弾力性の高いアーキテクチャの設計

AWS Solution Architect Associate 試験対策問題集

---

## 問題 1

２６ 複数の内部サービスが同じRDSインスタンスへ大量接続しており、スパイク時に接続数上限が問題になっています。アプリケーションの接続管理を簡素化しつつ、可用性を保ちたい場合、加えて、将来的なトラフィック増加も見込まれているため、可用性だけでなく拡張性と運用のしやすさも同時に満たす設計が求められています。最も適切な方法はどれですか？

### 選択肢

**ア.** CloudFrontを前段に追加し、同じSQLの応答をエッジでキャッシュしてDB接続を減らす

**イ.** RDS Proxyを導入して接続プールを集約し、アプリケーションからの接続管理負荷を軽減する

**ウ.** RDSをEC2自己管理DBへ移行し、OSレベルで接続数を無制限に近づける

**エ.** 各サービスに接続再利用ライブラリを実装し、データベース側の構成は変更しない

<details>
<summary>解答と解説を表示</summary>

**正解:** イ. RDS Proxyを導入して接続プールを集約し、アプリケーションからの接続管理負荷を軽減する

#### 解説

多数の接続を効率化したいなら、RDS Proxyが接続プールの集約に有効です。


#### 各選択肢のポイント


**ア.**

- 不正解。コンテンツ配信とキャッシュには使えますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**イ.**

- 正解。多数の接続を効率化したいなら、RDS Proxyが接続プールの集約に有効です。


**ウ.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**エ.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。

</details>

---

## 問題 2

Route 53でフェイルオーバールーティングを構成しています。プライマリのWebアプリに障害が起きたときだけセカンダリへ切り替えたい場合、最も重要な設定はどれですか？さらに、障害時の影響を局所化し、復旧にかかる手順をできるだけ標準化したいと考えており、追加の運用負荷は最小限に抑える必要があります。

### 選択肢

**ア.** TTLを長く設定し、切り替え回数を減らしてDNSの安定性を優先する

**イ.** Geo DNSを使い、障害発生時は地域単位で別リージョンへ強制的に迂回させる

**ウ.** プライマリとセカンダリの両方に同じ重みを付け、応答が速い方へ送る

**エ.** ヘルスチェックとフェイルオーバーレコードを設定し、プライマリが不健康なときだけセカンダリを返す

<details>
<summary>解答と解説を表示</summary>

**正解:** エ. ヘルスチェックとフェイルオーバーレコードを設定し、プライマリが不健康なときだけセカンダリを返す

#### 解説

フェイルオーバールーティングでは、ヘルスチェックとプライマリ/セカンダリレコードの設定が重要です。


#### 各選択肢のポイント


**ア.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**イ.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**ウ.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**エ.**

- 正解。フェイルオーバールーティングでは、ヘルスチェックとプライマリ/セカンダリレコードの設定が重要です。

</details>

---

## 問題 3

重要な注文メッセージを処理するキューシステムでは、障害調査のために順序を保ったまま失敗メッセージを後から確認したい要件があります。重複排除も必要な場合、最も適切なキュータイプはどれですか？加えて、将来的なトラフィック増加も見込まれているため、可用性だけでなく拡張性と運用のしやすさも同時に満たす設計が求められています。

### 選択肢

**ア.** SNSトピックを使い、購読側で順序を保証するため単一コンシューマーに限定する

**イ.** SQS標準キューを使い、メッセージ属性で順序番号を付けてアプリケーションで並べ替える

**ウ.** SQS FIFOキューを使い、順序保証と重複排除を組み合わせて処理する

**エ.** Kinesis Data Firehoseを使い、失敗メッセージはS3へ出力して順序保証を代替する

<details>
<summary>解答と解説を表示</summary>

**正解:** ウ. SQS FIFOキューを使い、順序保証と重複排除を組み合わせて処理する

#### 解説

順序保証と重複排除が必要なメッセージングには、SQS FIFOキューが適しています。


#### 各選択肢のポイント


**ア.**

- 不正解。Pub/Sub 配信には使えますが、この設問で必要な疎結合なキューイングを直接満たせないため適していません。


**イ.**

- 不正解。疎結合なキューイングには使えますが、この設問で必要な疎結合なキューイングを直接満たせないため適していません。


**ウ.**

- 正解。順序保証と重複排除が必要なメッセージングには、SQS FIFOキューが適しています。


**エ.**

- 不正解。ストリーム収集には使えますが、この設問で必要な疎結合なキューイングを直接満たせないため適していません。

</details>

---

## 問題 4

バックエンド処理はピーク時に注文メッセージを受け取ります。受信側の処理が一時的に遅くなってもフロントエンドを止めず、後で確実に処理したい場合、また、同社は、単一障害点を避けつつ、障害発生時でもサービス継続性を高めたいと考えていますが、運用チームの人数は限られているため複雑な自前運用は避けたい状況です。最も適切な設計はどれですか？

### 選択肢

**ア.** 注文イベントをSQSキューへ入れてフロントエンドと処理系を疎結合にし、ワーカーが後続処理を行う

**イ.** フロントエンドから各ワーカーEC2へ順番にHTTPリクエストを送り、最後に応答したノードへだけ保存する

**ウ.** すべての注文をCloudWatch Logsへ出力し、ログサブスクリプションで非同期処理へ横流しする

**エ.** 注文リクエストを同期的にRDSへ直接書き込み、タイムアウト時はクライアントへ再送させる

<details>
<summary>解答と解説を表示</summary>

**正解:** ア. (詳細は各選択肢のポイントを参照)

#### 解説

スパイク吸収と疎結合化にはSQSキューを介した非同期設計が適しています。


#### 各選択肢のポイント


**ア.**

- 正解。スパイク吸収と疎結合化にはSQSキューを介した非同期設計が適しています。


**イ.**

- 不正解。一部の要件には対応できますが、この設問で必要な疎結合なキューイングを直接満たせないため適していません。


**ウ.**

- 不正解。ログ収集には使えますが、この設問で必要な疎結合なキューイングを直接満たせないため適していません。


**エ.**

- 不正解。一部の要件には対応できますが、この設問で必要な疎結合なキューイングを直接満たせないため適していません。

</details>

---

## 問題 5

本番システムでは、パッチ適用やメンテナンス時にもサービス継続が必要です。単一インスタンスに依存しない構成へ近づける第一歩として、また、同社は、単一障害点を避けつつ、障害発生時でもサービス継続性を高めたいと考えていますが、運用チームの人数は限られているため複雑な自前運用は避けたい状況です。最も適切なのはどれですか？

### 選択肢

**ア.** メンテナンス時間帯を深夜に固定し、サービス停止の告知だけを徹底する

**イ.** Auto Scalingグループに複数インスタンスを配置し、ALBなどで分散してローリング更新できるようにする

**ウ.** 単一EC2インスタンスのEBSを高速化し、OSパッチ適用時間を短縮する

**エ.** Route 53のTTLを短くして、障害時に別インスタンスのIPへ手動切り替えしやすくする

<details>
<summary>解答と解説を表示</summary>

**正解:** イ. Auto Scalingグループに複数インスタンスを配置し、ALBなどで分散してローリング更新できるようにする

#### 解説

単一インスタンス依存を減らすには、複数インスタンス化とロードバランシングが基本です。


#### 各選択肢のポイント


**ア.**

- 不正解。一部の要件には対応できますが、この設問で必要なHTTP/HTTPS の負荷分散を直接満たせないため適していません。


**イ.**

- 正解。単一インスタンス依存を減らすには、複数インスタンス化とロードバランシングが基本です。


**ウ.**

- 不正解。一部の要件には対応できますが、この設問で必要なHTTP/HTTPS の負荷分散を直接満たせないため適していません。


**エ.**

- 不正解。手作業に依存し、設定漏れや運用負荷が増えやすいため適していません。

</details>

---

## 問題 6

マルチリージョンで可用性を高めたいグローバルサービスがあります。ユーザーは最も近い正常なリージョンへ到達すればよく、リージョン障害時は自動で別リージョンへ切り替えたい場合、最も適切なDNS設計はどれですか？加えて、将来的なトラフィック増加も見込まれているため、可用性だけでなく拡張性と運用のしやすさも同時に満たす設計が求められています。

### 選択肢

**ア.** Route 53のレイテンシールーティングとヘルスチェックを使い、最も遅延の少ない正常リージョンへ送る

**イ.** CloudFrontディストリビューションを単一リージョンのALBへ向け、障害時は手動でオリジンを変更する

**ウ.** Route 53のフェイルオーバールーティングだけを使い、常にプライマリリージョンへ送って障害時だけセカンダリへ切り替える

**エ.** Global Acceleratorを使わず、各リージョンのALB DNS名をアプリへ事前配布してクライアント側で選択させる

<details>
<summary>解答と解説を表示</summary>

**正解:** ア. (詳細は各選択肢のポイントを参照)

#### 解説

最も近い正常リージョンへ自動的に送るには、Route 53のレイテンシールーティングとヘルスチェックが適しています。


#### 各選択肢のポイント


**ア.**

- 正解。最も近い正常リージョンへ自動的に送るには、Route 53のレイテンシールーティングとヘルスチェックが適しています。


**イ.**

- 不正解。手作業に依存し、設定漏れや運用負荷が増えやすいため適していません。


**ウ.**

- 不正解。DNS ルーティングやヘルスチェックには使えますが、この設問で必要なDNS ルーティングやヘルスチェックを直接満たせないため適していません。


**エ.**

- 不正解。HTTP/HTTPS の負荷分散には使えますが、この設問で必要なDNS ルーティングやヘルスチェックを直接満たせないため適していません。

</details>

---

## 問題 7

一部のワークロードは、同じメッセージが重複して配信されても結果が壊れないよう設計する必要があります。分散システムにおいて重複配信や再試行を前提に、障害復旧しやすい設計として最も重要なのはどれですか？加えて、将来的なトラフィック増加も見込まれているため、可用性だけでなく拡張性と運用のしやすさも同時に満たす設計が求められています。

### 選択肢

**ア.** 処理を冪等に設計し、同じリクエストやメッセージを複数回受けても結果が一貫するようにする

**イ.** メッセージIDの重複は監視対象にせず、後からバッチで整合性を取る方針にする

**ウ.** 重複が起きないようにすべて同期通信へ変更し、タイムアウトを長くして再試行を減らす

**エ.** データベースへの書き込み前に必ず手動承認を入れ、重複の可能性があるリクエストを人手判定する

<details>
<summary>解答と解説を表示</summary>

**正解:** ア. (詳細は各選択肢のポイントを参照)

#### 解説

障害や再試行が前提の分散システムでは、冪等性の確保が重要です。


#### 各選択肢のポイント


**ア.**

- 正解。障害や再試行が前提の分散システムでは、冪等性の確保が重要です。


**イ.**

- 不正解。一部の要件には対応できますが、この設問で必要な重複実行時も結果を一貫させる設計を直接満たせないため適していません。


**ウ.**

- 不正解。一部の要件には対応できますが、この設問で必要な重複実行時も結果を一貫させる設計を直接満たせないため適していません。


**エ.**

- 不正解。手作業に依存し、設定漏れや運用負荷が増えやすいため適していません。

</details>

---

## 問題 8

複数のEC2インスタンスから構成されるアプリケーションは、起動後にセッション情報をローカルメモリへ保持しています。インスタンス障害やスケールインがあってもユーザーセッションを維持しやすくしたい場合、最も適切な改善はどれですか？さらに、障害時の影響を局所化し、復旧にかかる手順をできるだけ標準化したいと考えており、追加の運用負荷は最小限に抑える必要があります。

### 選択肢

**ア.** ALBのスティッキーセッションを長時間有効化し、各ユーザーを可能な限り同じEC2へ固定する

**イ.** セッション情報をElastiCache for Redisなどの外部共有ストアへ移し、アプリサーバーをステートレス化する

**ウ.** 各インスタンスのEBSボリュームへセッションを書き出し、障害時は別インスタンスへEBSを付け替える

**エ.** Auto Scalingグループの最小台数を1台にして、スケールインによるセッション消失を防ぐ

<details>
<summary>解答と解説を表示</summary>

**正解:** イ. セッション情報をElastiCache for Redisなどの外部共有ストアへ移し、アプリサーバーをステートレス化する

#### 解説

可用性を高めるには、アプリサーバーをステートレスにしてセッションを共有ストアへ外出しするのが適切です。


#### 各選択肢のポイント


**ア.**

- 不正解。HTTP/HTTPS の負荷分散には使えますが、この設問で必要な共有キャッシュやセッション保管を直接満たせないため適していません。


**イ.**

- 正解。可用性を高めるには、アプリサーバーをステートレスにしてセッションを共有ストアへ外出しするのが適切です。


**ウ.**

- 不正解。一部の要件には対応できますが、この設問で必要な共有キャッシュやセッション保管を直接満たせないため適していません。


**エ.**

- 不正解。負荷に応じた台数調整には使えますが、この設問で必要な共有キャッシュやセッション保管を直接満たせないため適していません。

</details>

---

## 問題 9

企業は、注文イベントを複数の下流システムへ配信したいと考えています。会計、通知、分析の各システムは独立して処理し、ある購読先の障害が他に影響しないようにしたい場合、最も適切な構成はどれですか？さらに、障害時の影響を局所化し、復旧にかかる手順をできるだけ標準化したいと考えており、追加の運用負荷は最小限に抑える必要があります。

### 選択肢

**ア.** SNSトピックへイベントを発行し、各下流システムは個別のSQSキューやエンドポイントで購読する

**イ.** DynamoDB Streamsを直接3つのシステムへ接続し、同一ストリームから並列に読み取らせる

**ウ.** EventBridgeを使わず、注文APIから会計、通知、分析の各エンドポイントへ順次同期呼び出しする

**エ.** 1つのSQSキューへすべての下流システムを接続し、各システムが必要なメッセージだけを読み飛ばす

<details>
<summary>解答と解説を表示</summary>

**正解:** ア. (詳細は各選択肢のポイントを参照)

#### 解説

複数の独立購読先へ疎結合に配信し、障害の影響を分離するにはSNSと個別購読先の組み合わせが適しています。


#### 各選択肢のポイント


**ア.**

- 正解。複数の独立購読先へ疎結合に配信し、障害の影響を分離するにはSNSと個別購読先の組み合わせが適しています。


**イ.**

- 不正解。DynamoDB 変更イベントの取得には使えますが、この設問で必要な疎結合なキューイングを直接満たせないため適していません。


**ウ.**

- 不正解。イベントルーティングには使えますが、この設問で必要な疎結合なキューイングを直接満たせないため適していません。


**エ.**

- 不正解。疎結合なキューイングには使えますが、この設問で必要な疎結合なキューイングを直接満たせないため適していません。

</details>

---

## 問題 10

オンプレミスからAWSへバックアップを複製しています。ランサムウェアや誤削除に備え、バックアップ自体の保護も強めたい場合、最も適切な考え方はどれですか？さらに、障害時の影響を局所化し、復旧にかかる手順をできるだけ標準化したいと考えており、追加の運用負荷は最小限に抑える必要があります。

### 選択肢

**ア.** Amazon EFSへバックアップを集約し、NFSアクセス制御で上書きを防止する

**イ.** バックアップがあること自体が保護になるため、保持期間を短くして毎日新しいバックアップへ置き換える

**ウ.** すべてのバックアップを同一アカウントの単一S3バケットへ保管し、削除は管理者のみ許可する

**エ.** AWS Backupのバックアップボールトを使い、必要に応じてVault Lockやクロスアカウントコピーを組み合わせる

<details>
<summary>解答と解説を表示</summary>

**正解:** エ. AWS Backupのバックアップボールトを使い、必要に応じてVault Lockやクロスアカウントコピーを組み合わせる

#### 解説

バックアップ自体の保護を強めるには、AWS Backupの機能やVault Lock、別アカウントコピーが有効です。


#### 各選択肢のポイント


**ア.**

- 不正解。共有ファイルストレージには使えますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**イ.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**ウ.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**エ.**

- 正解。バックアップ自体の保護を強めるには、AWS Backupの機能やVault Lock、別アカウントコピーが有効です。

</details>

---

## 問題 11

クライアントからの注文APIは、短時間に同じリクエストが複数回送られる可能性があります。ネットワーク障害時の再送でも重複注文を防ぎたい場合、また、現在の運用手順は手作業が多く、担当者依存を減らしながら障害耐性を高めたいという要望があります。最も適切な設計はどれですか？

### 選択肢

**ア.** クライアントごとに固定の接続を維持し、再送が起きないようTCPセッションを長寿命化する

**イ.** API Gatewayのスロットリングだけを設定し、同時リクエスト数を減らして重複登録を防ぐ

**ウ.** 注文作成APIに冪等キーを導入し、同じ要求識別子に対しては同一結果を返すようにする

**エ.** 重複は後続の会計処理で見つけて取消す前提とし、受付APIでは毎回新規注文として登録する

<details>
<summary>解答と解説を表示</summary>

**正解:** ウ. 注文作成APIに冪等キーを導入し、同じ要求識別子に対しては同一結果を返すようにする

#### 解説

再送や重複送信に備えるには、冪等キーを使って同じ要求を一意に扱う設計が適切です。


#### 各選択肢のポイント


**ア.**

- 不正解。一部の要件には対応できますが、この設問で必要な重複実行時も結果を一貫させる設計を直接満たせないため適していません。


**イ.**

- 不正解。一部の要件には対応できますが、この設問で必要な重複実行時も結果を一貫させる設計を直接満たせないため適していません。


**ウ.**

- 正解。再送や重複送信に備えるには、冪等キーを使って同じ要求を一意に扱う設計が適切です。


**エ.**

- 不正解。一部の要件には対応できますが、この設問で必要な重複実行時も結果を一貫させる設計を直接満たせないため適していません。

</details>

---

## 問題 12

企業は、マイクロサービス間のイベント連携を行っています。各サービスは独立しており、将来新しい購読先が追加されても既存送信側の変更を減らしたい場合、また、同社は、単一障害点を避けつつ、障害発生時でもサービス継続性を高めたいと考えていますが、運用チームの人数は限られているため複雑な自前運用は避けたい状況です。最も適切な設計はどれですか？

### 選択肢

**ア.** 各マイクロサービスへ共通ライブラリを配布し、そのライブラリ内で受信先一覧を固定化して運用する

**イ.** イベント内容を共有データベースへ書き込み、各サービスが同じテーブルをポーリングして受信する

**ウ.** イベントバスまたはトピックを中心にしたパブリッシュ/サブスクライブ設計へし、送信側はイベント発行だけを行う

**エ.** 送信側サービスからすべての購読先APIを順次呼び出す設計にし、追加先が増えたら送信側ロジックへ組み込む

<details>
<summary>解答と解説を表示</summary>

**正解:** ウ. イベントバスまたはトピックを中心にしたパブリッシュ/サブスクライブ設計へし、送信側はイベント発行だけを行う

#### 解説

疎結合で購読先追加にも強いのは、イベントバスやトピックを中心としたPub/Sub設計です。


#### 各選択肢のポイント


**ア.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**イ.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**ウ.**

- 正解。疎結合で購読先追加にも強いのは、イベントバスやトピックを中心としたPub/Sub設計です。


**エ.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。

</details>

---

## 問題 13

オブジェクトストレージに保存された重要ファイルについて、リージョン障害が発生しても別リージョンで復旧できるようにしたい企業があります。更新頻度は低く、RPOを小さく保ちたい場合、また、現在の運用手順は手作業が多く、担当者依存を減らしながら障害耐性を高めたいという要望があります。最も適切な方法はどれですか？

### 選択肢

**ア.** S3バージョニングだけを有効化し、リージョン障害時は同じバケットから過去版を読み出す

**イ.** S3クロスリージョンレプリケーションを設定し、別リージョンへ継続複製する

**ウ.** EBSスナップショットを取得して別リージョンのEC2へアタッチできるよう準備する

**エ.** CloudFrontでオブジェクトを長期間キャッシュし、元リージョン障害時はエッジキャッシュを恒久データソースとして扱う

<details>
<summary>解答と解説を表示</summary>

**正解:** イ. S3クロスリージョンレプリケーションを設定し、別リージョンへ継続複製する

#### 解説

リージョン障害に備えてS3データを別リージョンへ継続的に複製するには、CRRが適しています。


#### 各選択肢のポイント


**ア.**

- 不正解。削除や上書きからの復旧性向上には使えますが、この設問で必要なS3 オブジェクトの別リージョン複製を直接満たせないため適していません。


**イ.**

- 正解。リージョン障害に備えてS3データを別リージョンへ継続的に複製するには、CRRが適しています。


**ウ.**

- 不正解。ブロックストレージのバックアップには使えますが、この設問で必要なS3 オブジェクトの別リージョン複製を直接満たせないため適していません。


**エ.**

- 不正解。コンテンツ配信とキャッシュには使えますが、この設問で必要なS3 オブジェクトの別リージョン複製を直接満たせないため適していません。

</details>

---

## 問題 14

多層アプリケーションでは、Web層、アプリ層、DB層がそれぞれ別サブネットにあります。単一のAZ障害が発生してもサービス継続したい場合、最も適切なサブネット設計はどれですか？加えて、将来的なトラフィック増加も見込まれているため、可用性だけでなく拡張性と運用のしやすさも同時に満たす設計が求められています。

### 選択肢

**ア.** 各層のサブネットを1つのAZにだけ作成し、障害時は別AZへ手動で新しいサブネットを作る

**イ.** DB層だけ複数AZにし、Web層とアプリ層はAuto Scalingで補完する設計に寄せる

**ウ.** Web層だけ複数AZに分散し、アプリ層とDB層は運用簡素化のため単一AZへ集約する

**エ.** 各層のサブネットを少なくとも2つのAZに分散して配置し、依存サービスもマルチAZで構成する

<details>
<summary>解答と解説を表示</summary>

**正解:** エ. 各層のサブネットを少なくとも2つのAZに分散して配置し、依存サービスもマルチAZで構成する

#### 解説

AZ障害への耐性を持たせるには、各層を少なくとも2つのAZへ分散配置するのが基本です。


#### 各選択肢のポイント


**ア.**

- 不正解。手作業に依存し、設定漏れや運用負荷が増えやすいため適していません。


**イ.**

- 不正解。負荷に応じた台数調整には使えますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**ウ.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**エ.**

- 正解。AZ障害への耐性を持たせるには、各層を少なくとも2つのAZへ分散配置するのが基本です。

</details>

---

## 問題 15

データベース層の障害時にRPOを小さく保ちながら、迅速に復旧できる運用を整えたい企業があります。フルバックアップだけでなく継続的な変更履歴も必要な場合、さらに、障害時の影響を局所化し、復旧にかかる手順をできるだけ標準化したいと考えており、追加の運用負荷は最小限に抑える必要があります。最も適切な方法はどれですか？

### 選択肢

**ア.** 週次スナップショットだけを取得し、障害時は最新スナップショットから復元する

**イ.** リードレプリカを主な復旧手段とし、スナップショット取得頻度は抑えて運用する

**ウ.** CloudWatchメトリクスを保存し、障害後はメトリクスの変化からデータを推定して再作成する

**エ.** ポイントインタイムリカバリに対応したバックアップ設定を有効化し、必要時に任意時点へ復元する

<details>
<summary>解答と解説を表示</summary>

**正解:** エ. ポイントインタイムリカバリに対応したバックアップ設定を有効化し、必要時に任意時点へ復元する

#### 解説

RPOを小さくして任意時点復元したいなら、PITRに対応したバックアップ設定が適しています。


#### 各選択肢のポイント


**ア.**

- 不正解。手作業に依存し、設定漏れや運用負荷が増えやすいため適していません。


**イ.**

- 不正解。読み取り性能の向上や参照分散には使えますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**ウ.**

- 不正解。事後の可視化や検知には使えますが、この設問で必要な統制や自動化の中核にはなりません。


**エ.**

- 正解。RPOを小さくして任意時点復元したいなら、PITRに対応したバックアップ設定が適しています。

</details>

---

## 問題 16

ECサイトは単一AZのRDS for MySQLで注文データを管理しています。インスタンス障害時も自動フェイルオーバーで可用性を高めたい一方、アプリケーションの変更は最小限にしたい場合、加えて、将来的なトラフィック増加も見込まれているため、可用性だけでなく拡張性と運用のしやすさも同時に満たす設計が求められています。最も適切な方法はどれですか？

### 選択肢

**ア.** RDSをMulti-AZ構成へ変更し、スタンバイへの自動フェイルオーバーを利用する

**イ.** 同一AZ内にリードレプリカを追加し、障害時は手動でプライマリへ昇格する

**ウ.** RDSスナップショットを1時間ごとに取得し、障害時は最新スナップショットから復元する

**エ.** EC2上のMySQLへ移行し、Auto ScalingグループでDBサーバーを複数台起動する

<details>
<summary>解答と解説を表示</summary>

**正解:** ア. (詳細は各選択肢のポイントを参照)

#### 解説

RDSの可用性をアプリ変更最小で高めるには、Multi-AZ構成が最適です。


#### 各選択肢のポイント


**ア.**

- 正解。RDSの可用性をアプリ変更最小で高めるには、Multi-AZ構成が最適です。


**イ.**

- 不正解。手作業に依存し、設定漏れや運用負荷が増えやすいため適していません。


**ウ.**

- 不正解。一部の要件には対応できますが、この設問で必要なAZ 障害に備えた高可用化を直接満たせないため適していません。


**エ.**

- 不正解。負荷に応じた台数調整には使えますが、この設問で必要なAZ 障害に備えた高可用化を直接満たせないため適していません。

</details>

---

## 問題 17

サーバーレスアプリケーションでは、受け付けたリクエストを処理するLambda関数が失敗することがあります。処理失敗時の再試行後も失われないように、後で原因調査できる仕組みを入れたい場合、また、同社は、単一障害点を避けつつ、障害発生時でもサービス継続性を高めたいと考えていますが、運用チームの人数は限られているため複雑な自前運用は避けたい状況です。最も適切な方法はどれですか？

### 選択肢

**ア.** Lambdaのタイムアウトを長くし、失敗したリクエストはクライアント側の再試行だけに任せる

**イ.** CloudWatchメトリクスでエラー数を監視し、失敗イベント本体は保存しない

**ウ.** 同じLambda関数を2回連続で呼び出し、どちらか一方が成功すれば処理完了とみなす

**エ.** 失敗したイベントを保存するためにDLQまたはオンフェイラーデスティネーションを設定する

<details>
<summary>解答と解説を表示</summary>

**正解:** エ. (詳細は各選択肢のポイントを参照)

#### 解説

非同期処理の失敗イベントを失わず後で調査したいなら、DLQやオンフェイラーデスティネーションの設定が適しています。


#### 各選択肢のポイント


**ア.**

- 不正解。一部の要件には対応できますが、この設問で必要な失敗イベントの退避を直接満たせないため適していません。


**イ.**

- 不正解。メトリクス監視には使えますが、この設問で必要な失敗イベントの退避を直接満たせないため適していません。


**ウ.**

- 不正解。一部の要件には対応できますが、この設問で必要な失敗イベントの退避を直接満たせないため適していません。


**エ.**

- 正解。非同期処理の失敗イベントを失わず後で調査したいなら、DLQやオンフェイラーデスティネーションの設定が適しています。

</details>

---

## 問題 18

画像処理ワーカーがSQSキューからメッセージを取得して処理しています。処理時間が長く、ワーカー障害時には別ワーカーで再処理したい一方、処理中のメッセージが早すぎて再配信されることは避けたい場合、最も適切な設定はどれですか？さらに、障害時の影響を局所化し、復旧にかかる手順をできるだけ標準化したいと考えており、追加の運用負荷は最小限に抑える必要があります。

### 選択肢

**ア.** メッセージ保持期間を短くし、処理時間を超えたメッセージは自動削除して重複処理を防ぐ

**イ.** 長時間処理の代わりにポーリング間隔だけを延ばし、取得後は即座にメッセージを削除する

**ウ.** デッドレターキューだけを設定し、可視性タイムアウトはデフォルトのまま運用する

**エ.** 可視性タイムアウトを実際の処理時間に合わせて設定し、必要に応じて延長できるようにする

<details>
<summary>解答と解説を表示</summary>

**正解:** エ. 可視性タイムアウトを実際の処理時間に合わせて設定し、必要に応じて延長できるようにする

#### 解説

長時間処理のSQSワーカーでは、可視性タイムアウトを適切に設定して早すぎる再配信を防ぐ必要があります。


#### 各選択肢のポイント


**ア.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**イ.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**ウ.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**エ.**

- 正解。長時間処理のSQSワーカーでは、可視性タイムアウトを適切に設定して早すぎる再配信を防ぐ必要があります。

</details>

---

## 問題 19

アプリケーションはAmazon EFS上の共有ファイルを複数AZのEC2から利用しています。AZ障害が発生しても共有データへ継続アクセスしたい場合、また、同社は、単一障害点を避けつつ、障害発生時でもサービス継続性を高めたいと考えていますが、運用チームの人数は限られているため複雑な自前運用は避けたい状況です。最も適切な設計はどれですか？

### 選択肢

**ア.** EFSは可用性より性能を優先して使い、AZ障害時はアプリケーションの書き込みを停止する

**イ.** EFSを標準ストレージクラスで構成し、各AZのEC2からマウントターゲット経由でアクセスする

**ウ.** 各AZごとに別々のEBSボリュームを用意し、rsyncで共有ファイルを同期する

**エ.** EFSを単一AZへ限定し、障害時は同一ファイルをS3から手動復元して各EC2へ配布する

<details>
<summary>解答と解説を表示</summary>

**正解:** イ. EFSを標準ストレージクラスで構成し、各AZのEC2からマウントターゲット経由でアクセスする

#### 解説

EFSはマルチAZで利用でき、各AZのマウントターゲット経由で共有データへ継続アクセスできます。


#### 各選択肢のポイント


**ア.**

- 不正解。共有ファイルストレージには使えますが、この設問で必要な共有ファイルストレージを直接満たせないため適していません。


**イ.**

- 正解。EFSはマルチAZで利用でき、各AZのマウントターゲット経由で共有データへ継続アクセスできます。


**ウ.**

- 不正解。一部の要件には対応できますが、この設問で必要な共有ファイルストレージを直接満たせないため適していません。


**エ.**

- 不正解。手作業に依存し、設定漏れや運用負荷が増えやすいため適していません。

</details>

---

## 問題 20

企業は、Auroraデータベースを使った読み取り中心のアプリケーションを運用しています。可用性と読み取りスケーラビリティを高めつつ、プライマリ障害時には迅速に切り替えたい場合、また、現在の運用手順は手作業が多く、担当者依存を減らしながら障害耐性を高めたいという要望があります。最も適切な設計はどれですか？

### 選択肢

**ア.** Auroraリーダーインスタンスを複数AZに配置し、アプリケーションはリーダーエンドポイントとライターエンドポイントを用途別に使い分ける

**イ.** Redshiftへ定期複製して参照クエリを逃がし、障害時は別途切り替え手順で運用する

**ウ.** RDS for MySQLの単一AZ構成へ移行し、読み取りはCloudFrontのキャッシュで代替する

**エ.** 1台のAuroraライターインスタンスだけを大きくし、リードレプリカは使わず垂直スケールで対応する

<details>
<summary>解答と解説を表示</summary>

**正解:** ア. (詳細は各選択肢のポイントを参照)

#### 解説

Auroraではリーダーを複数AZへ配置し、用途別エンドポイントを使うことで可用性と読み取り性能を高められます。


#### 各選択肢のポイント


**ア.**

- 正解。Auroraではリーダーを複数AZへ配置し、用途別エンドポイントを使うことで可用性と読み取り性能を高められます。


**イ.**

- 不正解。一部の要件には対応できますが、この設問で必要な高可用なマネージド RDBを直接満たせないため適していません。


**ウ.**

- 不正解。コンテンツ配信とキャッシュには使えますが、この設問で必要な高可用なマネージド RDBを直接満たせないため適していません。


**エ.**

- 不正解。読み取り性能の向上や参照分散には使えますが、この設問で必要な高可用なマネージド RDBを直接満たせないため適していません。

</details>

---

## 問題 21

アプリケーションはECS on Fargateで稼働しています。サービス更新時に無停止に近い形で新しいタスクへ切り替えたい場合、また、同社は、単一障害点を避けつつ、障害発生時でもサービス継続性を高めたいと考えていますが、運用チームの人数は限られているため複雑な自前運用は避けたい状況です。最も適切な方法はどれですか？

### 選択肢

**ア.** 新旧のタスクを同じターゲットグループへ常に100%登録し、アプリケーション側で版判定を行う

**イ.** Route 53のAレコードをデプロイのたびに別サービスへ付け替え、コンテナヘルスチェックは行わない

**ウ.** ローリングデプロイまたはBlue/Greenデプロイを使い、ALBヘルスチェックを見ながら段階的に切り替える

**エ.** 古いタスクをすべて停止してから新しいタスクを起動し、デプロイ時間を短くする

<details>
<summary>解答と解説を表示</summary>

**正解:** ウ. ローリングデプロイまたはBlue/Greenデプロイを使い、ALBヘルスチェックを見ながら段階的に切り替える

#### 解説

無停止に近い更新には、ローリングまたはBlue/GreenデプロイとALBヘルスチェックの組み合わせが適しています。


#### 各選択肢のポイント


**ア.**

- 不正解。一部の要件には対応できますが、この設問で必要なHTTP/HTTPS の負荷分散を直接満たせないため適していません。


**イ.**

- 不正解。DNS ルーティングやヘルスチェックには使えますが、この設問で必要なHTTP/HTTPS の負荷分散を直接満たせないため適していません。


**ウ.**

- 正解。無停止に近い更新には、ローリングまたはBlue/GreenデプロイとALBヘルスチェックの組み合わせが適しています。


**エ.**

- 不正解。一部の要件には対応できますが、この設問で必要なHTTP/HTTPS の負荷分散を直接満たせないため適していません。

</details>

---

## 問題 22

企業は、複数AZにまたがるWebアプリケーションを運用しています。片方のAZで障害が発生してもトラフィックを自動で健全なインスタンスへ振り分けたい場合、最も適切な構成はどれですか？また、現在の運用手順は手作業が多く、担当者依存を減らしながら障害耐性を高めたいという要望があります。

### 選択肢

**ア.** NLBを単一AZへ配置し、バックエンドインスタンスだけを複数AZに分散して可用性を確保する

**イ.** 単一AZのALBと複数AZのEC2を組み合わせ、障害時はRoute 53のDNSキャッシュ更新に期待する

**ウ.** 複数AZにまたがるALBとAuto Scalingグループを使い、ヘルスチェックに基づいて健全なターゲットへルーティングする

**エ.** CloudFrontのオリジンを単一のEC2インスタンスへ向け、障害時は手動で別AZのインスタンスへ切り替える

<details>
<summary>解答と解説を表示</summary>

**正解:** ウ. 複数AZにまたがるALBとAuto Scalingグループを使い、ヘルスチェックに基づいて健全なターゲットへルーティングする

#### 解説

ALBとAuto Scalingグループを複数AZで構成することで、AZ障害時にも健全なターゲットへ自動分散できます。


#### 各選択肢のポイント


**ア.**

- 不正解。L4 負荷分散には使えますが、この設問で必要なHTTP/HTTPS の負荷分散を直接満たせないため適していません。


**イ.**

- 不正解。DNS ルーティングやヘルスチェックには使えますが、この設問で必要なHTTP/HTTPS の負荷分散を直接満たせないため適していません。


**ウ.**

- 正解。ALBとAuto Scalingグループを複数AZで構成することで、AZ障害時にも健全なターゲットへ自動分散できます。


**エ.**

- 不正解。手作業に依存し、設定漏れや運用負荷が増えやすいため適していません。

</details>

---

## 問題 23

企業は、アプリケーションの設定値を運用中に変更することがあります。インスタンス再作成やコード再デプロイをしなくても設定を取り出せるようにしたい場合、さらに、障害時の影響を局所化し、復旧にかかる手順をできるだけ標準化したいと考えており、追加の運用負荷は最小限に抑える必要があります。最も適切な方法はどれですか？

### 選択肢

**ア.** 設定値をCloudWatch Logsへ出力し、アプリケーションは最新ログを検索して参照する

**イ.** 設定値をSystems Manager Parameter StoreまたはAppConfigで管理し、アプリケーションが実行時に取得する

**ウ.** 設定値をAMIへ焼き込み、変更が必要になったら新しいAMIを作成して全台を置き換える

**エ.** 設定値をRoute 53のTXTレコードへ保存し、アプリケーションはDNSクエリで取得する

<details>
<summary>解答と解説を表示</summary>

**正解:** イ. 設定値をSystems Manager Parameter StoreまたはAppConfigで管理し、アプリケーションが実行時に取得する

#### 解説

動的な設定管理にはParameter StoreやAppConfigの利用が適しています。


#### 各選択肢のポイント


**ア.**

- 不正解。ログ収集には使えますが、この設問で必要な設定値や簡易的なシークレットの保管を直接満たせないため適していません。


**イ.**

- 正解。動的な設定管理にはParameter StoreやAppConfigの利用が適しています。


**ウ.**

- 不正解。EC2 起動テンプレート用イメージには使えますが、この設問で必要な設定値や簡易的なシークレットの保管を直接満たせないため適していません。


**エ.**

- 不正解。DNS ルーティングやヘルスチェックには使えますが、この設問で必要な設定値や簡易的なシークレットの保管を直接満たせないため適していません。

</details>

---

## 問題 24

企業は、数分以内に処理が完了しなくてもよい大容量ファイル変換処理を受け付けています。リクエストはスパイクし、処理完了後に利用者へ通知できればよい場合、最も適切なアーキテクチャはどれですか？また、現在の運用手順は手作業が多く、担当者依存を減らしながら障害耐性を高めたいという要望があります。

### 選択肢

**ア.** 同期ALBの背後にEC2を置き、クライアントは変換完了まで長時間HTTP接続を維持する

**イ.** API GatewayとLambdaの同期実行だけで大容量変換を処理し、15分以内に終わらない処理はクライアントが再送する

**ウ.** アップロードをS3で受け、イベントをキューへ送り、ワーカーが非同期処理した後に通知する

**エ.** CloudFront Functionsでファイル変換を行い、エッジで完結させる

<details>
<summary>解答と解説を表示</summary>

**正解:** ウ. アップロードをS3で受け、イベントをキューへ送り、ワーカーが非同期処理した後に通知する

#### 解説

時間のかかるスパイク処理は、S3受信とキューを使った非同期ワークフローが適しています。


#### 各選択肢のポイント


**ア.**

- 不正解。HTTP/HTTPS の負荷分散には使えますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**イ.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**ウ.**

- 正解。時間のかかるスパイク処理は、S3受信とキューを使った非同期ワークフローが適しています。


**エ.**

- 不正解。コンテンツ配信とキャッシュには使えますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。

</details>

---

## 問題 25

複数の内部サービスが同じRDSインスタンスへ大量接続しており、スパイク時に接続数上限が問題になっています。アプリケーションの接続管理を簡素化しつつ、可用性を保ちたい場合、加えて、将来的なトラフィック増加も見込まれているため、可用性だけでなく拡張性と運用のしやすさも同時に満たす設計が求められています。最も適切な方法はどれですか？

### 選択肢

**ア.** RDSをEC2自己管理DBへ移行し、OSレベルで接続数を無制限に近づける

**イ.** 各サービスに接続再利用ライブラリを実装し、データベース側の構成は変更しない

**ウ.** CloudFrontを前段に追加し、同じSQLの応答をエッジでキャッシュしてDB接続を減らす

**エ.** RDS Proxyを導入して接続プールを集約し、アプリケーションからの接続管理負荷を軽減する

<details>
<summary>解答と解説を表示</summary>

**正解:** エ. RDS Proxyを導入して接続プールを集約し、アプリケーションからの接続管理負荷を軽減する

#### 解説

多数の接続を効率化したいなら、RDS Proxyが接続プールの集約に有効です。


#### 各選択肢のポイント


**ア.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**イ.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**ウ.**

- 不正解。コンテンツ配信とキャッシュには使えますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**エ.**

- 正解。多数の接続を効率化したいなら、RDS Proxyが接続プールの集約に有効です。

</details>

---

## 問題 26

複数リージョンから読み書きされるユーザープロファイルを低レイテンシで提供したいアプリケーションがあります。単一リージョン障害時も継続利用したい場合、最も適切なデータベース設計はどれですか？また、現在の運用手順は手作業が多く、担当者依存を減らしながら障害耐性を高めたいという要望があります。

### 選択肢

**ア.** 単一リージョンのDynamoDBテーブルを使い、別リージョン障害時はアプリケーションを停止して復旧を待つ

**イ.** ElastiCache for Redisを永続ストアとして利用し、各リージョンにローカルキャッシュだけを持たせる

**ウ.** DynamoDBグローバルテーブルを使い、複数リージョンでアクティブ-アクティブにレプリケーションする

**エ.** RDS Multi-AZだけで複数リージョンからの低レイテンシ書き込みを実現する

<details>
<summary>解答と解説を表示</summary>

**正解:** ウ. DynamoDBグローバルテーブルを使い、複数リージョンでアクティブ-アクティブにレプリケーションする

#### 解説

複数リージョンから低レイテンシで読み書きし、リージョン障害にも備えるならDynamoDBグローバルテーブルが適しています。


#### 各選択肢のポイント


**ア.**

- 不正解。一部の要件には対応できますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**イ.**

- 不正解。共有キャッシュやセッション保管には使えますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。


**ウ.**

- 正解。複数リージョンから低レイテンシで読み書きし、リージョン障害にも備えるならDynamoDBグローバルテーブルが適しています。


**エ.**

- 不正解。AZ 障害に備えた高可用化には使えますが、この設問で必要な一部の用途には有効な構成を直接満たせないため適していません。

</details>

---

## 問題 27

自社の重要なS3データは誤削除やアプリケーションバグによる上書きに備えたい一方、リージョン障害時の復旧も考慮しています。最も包括的な対策はどれですか？加えて、将来的なトラフィック増加も見込まれているため、可用性だけでなく拡張性と運用のしやすさも同時に満たす設計が求められています。

### 選択肢

**ア.** S3 Standard-Infrequent Accessへ移行し、アクセス頻度を下げることで誤削除の可能性を小さくする

**イ.** CloudFrontキャッシュを長く保持し、元データ消失時はエッジキャッシュから復元する

**ウ.** S3イベントでPutObject時だけ別バケットへコピーし、DeleteObjectや上書きは復旧対象外とする

**エ.** S3バージョニングとクロスリージョンレプリケーションを組み合わせ、必要なら削除保護も検討する

<details>
<summary>解答と解説を表示</summary>

**正解:** エ. S3バージョニングとクロスリージョンレプリケーションを組み合わせ、必要なら削除保護も検討する

#### 解説

誤削除や上書きに備えるにはバージョニング、リージョン障害対策にはCRRが有効で、組み合わせると包括的です。


#### 各選択肢のポイント


**ア.**

- 不正解。一部の要件には対応できますが、この設問で必要なS3 オブジェクトの別リージョン複製を直接満たせないため適していません。


**イ.**

- 不正解。キャッシュは一時的な配信最適化であり、正式な復旧手段にはなりません。


**ウ.**

- 不正解。一部の要件には対応できますが、この設問で必要なS3 オブジェクトの別リージョン複製を直接満たせないため適していません。


**エ.**

- 正解。誤削除や上書きに備えるにはバージョニング、リージョン障害対策にはCRRが有効で、組み合わせると包括的です。

</details>

---
